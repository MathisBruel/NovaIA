server:
  port: 8080

spring:
  application:
    name: specialweek-backend
  datasource:
    url: jdbc:mysql://db:3306/specialweek?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: specialweek
    password: specialweek
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

